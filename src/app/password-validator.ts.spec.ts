import { PasswordValidatorTs } from './password-validator.ts';

describe('PasswordValidatorTs', () => {
  it('should create an instance', () => {
    expect(new PasswordValidatorTs()).toBeTruthy();
  });
});
